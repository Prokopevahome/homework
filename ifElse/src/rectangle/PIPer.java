package rectangle;

public class PIPer {
    int square(int x, int t, int s) {
        int plpov = (x * t + x * s + t * s) * 2;
        return plpov;
    }

    int volume(int x, int t, int s) {
        int vol = x * t * s;
        return
    }
}
